# Dependents Test Evidence

## Checklist

| Ticket | Evidence |
| --- | --- |
| BACK-266 | Requirements mapped in `dependents-test-matrix.md`. |
| BACK-267 | Unit tests cover domain and Create/Get/Update/Delete services. |
| BACK-268 | MVC, JPA and security integration tests cover HTTP validation, persistence and authorization. |
| BACK-269 | `DependentSystemFlowTest` covers create, consult, list, update and remove. |
| BACK-270 | JaCoCo is generated by `./mvnw verify`; analyze `target/site/jacoco`. |
| BACK-271 | PIT is configured for Dependents and executed with explicit Maven command. |
| BACK-272 | This evidence file records commands, reports and residual findings. |

## Execution Log

| Command | Result | Notes |
| --- | --- | --- |
| `./mvnw test` | Passed | 102 tests, 0 failures, 0 errors, 0 skipped. |
| `./mvnw verify` | Passed | 102 tests, 0 failures, 0 errors, 0 skipped; JaCoCo report generated in `target/site/jacoco`. |
| `./mvnw org.pitest:pitest-maven:mutationCoverage -DtargetClasses=com.jeepclub.backend.dependents.* -DtargetTests=com.jeepclub.backend.dependents.*` | Passed | 99 mutations generated, 98 killed, 1 survived, 0 with no coverage; PIT report generated in `target/pit-reports`. |

## Coverage Target

Minimum reference for the Dependents module:

| Metric | Target |
| --- | --- |
| Lines | 80% target; actual 91.88% (249/271) for `com.jeepclub.backend.dependents.*`. |
| Branches | 70% target; actual 72.64% (77/106) for `com.jeepclub.backend.dependents.*`. |

## Mutation Analysis

PIT result for `com.jeepclub.backend.dependents.*`:

| Metric | Result |
| --- | --- |
| Generated mutations | 99 |
| Killed mutations | 98 |
| Mutation score | 99% |
| Mutations without coverage | 0 |
| Test strength | 99% |

Residual survivor:

| Class | Method | Line | Classification | Analysis |
| --- | --- | --- | --- | --- |
| `Dependent` | `update` | 122 | Equivalent/infeasible through public contract | The surviving mutation negates `consentAccepted && !this.consentAccepted`. `Dependent.create` and `Dependent.reconstitute` both reject `consentAccepted=false`, so a persisted domain instance cannot reach `update` with `this.consentAccepted=false`. Consent revocation (`consentAccepted=false`) and repeated accepted consent are covered. |

No non-equivalent survivor remains in critical Dependents rules.

## Contract Check

Search executed:

`rg -n "dependents\\.core\\.domain\\.model\\.MedicalProfile|com\\.jeepclub\\.backend\\.dependents\\.core\\.domain\\.model\\.MedicalProfile" src/test/java src/test/resources`

Only the matrix note references the removed class name. New tests use the Health contract (`OwnerType.DEPENDENT`, `ownerId`) and do not depend on the removed Dependents medical profile model.

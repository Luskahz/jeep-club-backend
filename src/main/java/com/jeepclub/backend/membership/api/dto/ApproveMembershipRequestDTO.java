package com.jeepclub.backend.membership.api.dto;

// Aprovação não precisa de body — o ID vem na URL.
// Arquivo existe para extensão futura (ex: nota interna do admin).
public record ApproveMembershipRequestDTO() {}